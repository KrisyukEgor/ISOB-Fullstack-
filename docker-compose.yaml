services:
  db:
    image: postgres:16-alpine
    container_name: todo_postgres_db
    restart: always
    env_file:
      - ./backend/.env.dev
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: